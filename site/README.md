# 网页源文件快照

此目录保留智能找数目录、86 份独立报告及 3 份领域报告的 HTML/JSON，供审阅和维护。

为避免将大文件写入 Git 历史，媒体、预览资源、样本标签明细、完整记录分片和浏览器数据索引仅放在 Release 完整包。因此本目录不是可单独使用的完整网站。

使用完整功能，请从仓库 Releases 下载三个分卷，按根 README 合并并解压，再打开 `index.html`。

修改页面后，可将本目录内同相对路径文件覆盖到完整离线包中验证。不要改变页面对样本和报告的相对路径。
