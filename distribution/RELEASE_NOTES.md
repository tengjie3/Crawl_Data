# 智能找数 · 免安装 HTML 离线版

285 个数据集、855,000 条完整记录、86 份独立画像报告、3 份领域报告。支持实际样本预览、筛选、数据集对比和浏览器内合并下载。

## 下载方法

1. 下载 **三个 `.zip.part01` / `.zip.part02` / `.zip.part03` 分卷** 和 `download-helpers.zip`，放在同一目录。
2. 将 `download-helpers.zip` 解压到分卷旁边。Windows 双击 `join-windows.cmd`；macOS/Linux 运行 `sh join-macos-linux.command`。
3. 合并脚本自动校验 SHA256。校验通过后解压完整 ZIP，用 Chrome/Edge 打开其中 `index.html`。

**网页不需要 Python、Node、后台服务器或联网。不要下载 GitHub 的 Source code (zip) 作为完整网页包，它不含大型数据资源。**

三个分卷合计 3,975,647,640 Byte；解压后约 5.78 GB。下载、拼接和解压建议预留至少 15 GB 磁盘空间。

完整 ZIP SHA256：
`f6b1e227b2435dbc6fd2d86db6d4d032930ac2ceeb8d9b278ee32b5b1228788e`

## 验证与边界

- 公开副本脱敏了 57 条记录中的 2 个不同疑似第三方 API Key（139 处出现），不删除整条记录或媒体；完整处理记录随包交付，本地原件不变。
- 新公开包的报告、文本/图片/PDF/视频预览、对比、导出及实际合并下载再次通过离线浏览器测试。
- macOS/Chrome 的正常 `file://` 离线测试通过；310 段预览视频实际播放通过。
- 6,000 条记录合并：5,437 条保留、563 条重复、0 失败，输出文件逐一 SHA256 核验。
- 独立目录隔离测试通过，不依赖开发者原项目路径。Windows/Linux 未在真机完成端到端测试。
- 855,000 是跨数据集记录数，不是全局去重后的独立样本数。
- 本次发布保留历史评分，不代表官方 OpenDataArena 完整评测、训练增益或版权授权；旧 UI 的等权标签与归档总分权重存在差异，详见仓库 README。
- 数据权利、来源和许可按原记录保留，未知许可仍需复核。此包不包含 7×24 后台采集服务。
